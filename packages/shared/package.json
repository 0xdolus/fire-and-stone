{
  "name": "@fire-and-stone/shared",
  "private": true,
  "version": "0.0.0",
  "description": "Fire & Stone shared types, money helpers, and order status transitions. Bootstrap placeholder — no product code yet.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo \"packages/shared: no lint configured yet (bootstrap stage)\"",
    "typecheck": "echo \"packages/shared: no typecheck configured yet (bootstrap stage)\"",
    "test": "echo \"packages/shared: no tests configured yet (bootstrap stage)\""
  }
}
