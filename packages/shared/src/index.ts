/**
 * Fire & Stone — packages/shared
 *
 * Bootstrap placeholder. No shared types, money helpers, or status
 * transition logic have been implemented yet. This file exists so the
 * workspace has a valid TypeScript entry point for tooling (typecheck,
 * bundlers) to resolve during the bootstrap stage.
 *
 * See packages/shared/README.md and docs/architecture/Architecture-V1.md.
 */

export const BOOTSTRAP_PLACEHOLDER = true;
