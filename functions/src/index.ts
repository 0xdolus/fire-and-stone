/**
 * Fire & Stone — functions
 *
 * Bootstrap placeholder. No Cloud Functions (createOrder, updateOrderStatus,
 * cancelOrder, etc.) have been implemented yet. This file exists so the
 * workspace has a valid TypeScript entry point for tooling during the
 * bootstrap stage.
 *
 * Anything implemented here that writes orders or delivery slots is
 * High/Architectural risk — see functions/README.md and
 * docs/architecture/Repository-GitHub-Setup-V1.md §10.
 */

export const BOOTSTRAP_PLACEHOLDER = true;
